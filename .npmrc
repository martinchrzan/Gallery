# Refuse to install on an unsupported Node rather than letting it fail later.
#
# better-sqlite3 requires Node 22+, and on Node 20 its prebuilt binding loads
# and then segfaults the moment a database is opened — an exit code with no
# stack and nothing to search for. Failing at install time turns that into one
# clear line.
engine-strict=true
